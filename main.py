from fastapi import FastAPI

app = FastAPI()

# apps.include_router(employee_controller.router, prefix="/employees", tags=["employees"])
# apps.include_router(project_controller.router, prefix="/projects", tags=["projects"])
# apps.include_router(task_controller.router, prefix="/tasks", tags=["tasks"])
